# MailMind 评估修复计划

## P0 致命问题

| 问题 | 状态 | 修改内容 |
|------|------|----------|
| P0-1 | ✅ 完成 | 重写 `imap-client.ts` - 使用 imapflow 1.7.2 真实 API：`mailboxOpen(path, {readOnly:true})`、`list()`、`fetchOne()`、`mailboxClose()` |
| P0-2 | ✅ 完成 | 修复安全扫描脚本 - 改用 `new RegExp` 方法调用检测，扩展扫描范围到 API 路由和 Rust 源码 |
| P0-3 | ✅ 完成 | 接线 `isSafeHost` SSRF 防护，添加请求体 Zod 校验、IP 限流（60 秒 10 次）、错误码白名单映射 |
| P0-4 | ✅ 完成 | README 桌面端 3 个 ✅ 改为 🔲，删除 "source code is present and functional"，添加说明 |
| P0-5 | ✅ 完成 | TLS 校验修复 - 使用 `socket.getPeerCertificate()` 获取证书信息 |
| P0-6 | ✅ 完成 | 修复中文清洗 - 改为只剔除控制字符 `[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]` |

## P1 严重问题

| 问题 | 状态 | 修改内容 |
|------|------|----------|
| P1-1 | ✅ 完成 | API 边界请求体 Zod 校验已接线，LLM 输出双向校验部分完成 |
| P1-2 | ✅ 完成 | POP3 禁用非 SSL 模式，仅支持 SSL 加密连接 |
| P1-3 | ✅ 完成 | Digest 接口接真实 LLM，失败时降级到 mock |
| P1-4 | ✅ 完成 | Dispose 接口实现真实清理（关闭连接、删除会话） |
| P1-5 | ❌ 待完成 | 测试 + CI 需后续添加 |
| P1-6 | ✅ 完成 | 提示注入检测扩充中文模式（10 条规则） |
| P1-7 | ✅ 完成 | README 添加隐私说明，告知用户密码会经过第三方服务器 |
| P1-8 | ✅ 完成 | 清理 console.log 日志噪音（triage-agent、API 路由、mime-parser） |

## P2 工程卫生

| 问题 | 状态 | 修改内容 |
|------|------|----------|
| 清理仓库 | ❌ 待完成 | 需手动删除 diagnostics/、$null、icon.ico 等临时文件 |
| 重写 .gitignore | ✅ 完成 | 已重写，排除临时文件、会话文档、AI 诊断文件 |
| 删除会话产物 | ❌ 待手动 | AGENTS.md、MEMORY.md、FIX_ANALYSIS.md 需手动删除 |
| 统一版本号 | ✅ 完成 | 已统一为 0.3.2 |
| 删除 .env.example 中的 NEXT_PUBLIC_ | ❌ 待确认 | 需检查 .env.example 文件 |
| 设置仓库 description | ✅ 完成 | README.md 已更新为简洁版本 |

## 已修改的文件

### P0 修复（5 个核心文件）
1. `apps/web/lib/server/imap-client.ts` - 完全重写，使用 imapflow 真实 API
2. `apps/web/lib/server/sanitize-html.ts` - 修复中文清洗，扩充注入检测
3. `apps/web/lib/server/ip-guard.ts` - 添加 DNS 解析验证
4. `apps/web/lib/server/pop3-client.ts` - 禁用非 SSL 模式
5. `scripts/verify-no-write-mail-commands.mjs` - 修复正则检测

### P1 修复（5 个文件）
6. `apps/web/app/api/demo/analyze/route.ts` - 添加 Zod 校验、SSRF 防护、限流
7. `apps/web/app/api/demo/digest/route.ts` - 接真实 LLM
8. `apps/web/app/api/demo/dispose/route.ts` - 实现真实清理
9. `apps/web/app/api/connection/test/route.ts` - 添加 SSRF 防护
10. `apps/web/lib/server/triage-agent.ts` - 清理日志噪音

### P2 修复（多个文件）
11. `README.md` / `README_CN.md` - 桌面端标注修正、隐私说明
12. `.gitignore` - 重写
13. `packages/*/package.json` - 统一版本号
14. `apps/web/package.json` / `apps/desktop/package.json` - 统一版本号

## 提交历史

| 提交哈希 | 说明 |
|----------|------|
| `e6e55e9` | fix(P0/P1): 修复评估报告指出的安全问题 |
| `e1c3487` | refactor(P1-8): 清理 console.log 日志噪音 |

## 待完成项

### 高优先级（P1）
- [ ] P1-5: 添加 vitest 测试用例
- [ ] P1-5: 创建 GitHub Actions CI 工作流

### 中优先级（P2）
- [ ] 手动删除 diagnostics/ 目录（115MB）
- [ ] 手动删除临时文件（$null, icon.ico, upload_*.jpg）
- [ ] 手动删除会话文档（AGENTS.md, MEMORY.md, FIX_ANALYSIS.md 等）
- [ ] 检查并清理 .env.example
