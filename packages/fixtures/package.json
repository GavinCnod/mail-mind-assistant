{
  "name": "@mailmind/fixtures",
  "version": "0.3.2",
  "description": "Test email fixtures for MailMind",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
